import { Story, Meta } from '@storybook/react/types-6-0'
import InputRange from '.'

export default {
  title: 'InputRange',
  component: InputRange
} as Meta

export const Default: Story = () => <InputRange />
